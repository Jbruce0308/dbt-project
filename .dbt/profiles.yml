eth:
  outputs:
    ci:
      account: XJWOTTE-SF46071
      database: ci
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH')}}"
      role: ACCOUNTADMIN
      schema: ci_schema
      threads: 1
      type: snowflake
      user: DATABRUCE
      warehouse: compute_wh
  target: ci