version: 2

models:
  - name: stg_transactions
    description: > 
      this is gathering all the data from etherum blocktrain
    columns:
      - name: hash
        data_tests:
          - unique
          - not_null